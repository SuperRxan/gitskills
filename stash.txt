1. git stash
2. git pull
3. git stash pop

3. git stash the unstaged file

4. remote master create same file as local, test git pull

5. git push local













6. update by others in orion dev, so the commit become diversed.
